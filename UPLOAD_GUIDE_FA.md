# راهنمای خیلی کوتاه آپلود

1. فایل ZIP نهایی را Extract کن.
2. وارد پوشهٔ استخراج‌شده شو؛ باید همین دو مورد را در سطح اول ببینی: `.github` و `jeonghan-daily-review-bot-v2` (فایل راهنما هم کنارشان است).
3. تمام محتوای داخلش را در **ریشهٔ همان Repository** آپلود کن و برای فایل‌های هم‌نام گزینهٔ Replace/Commit را بزن. ساختار `.github/workflows/main.yml` و `jeonghan-daily-review-bot-v2/...` باید حفظ شود.
4. در GitHub وارد `Actions → Jeonghan Daily Review Bot → Run workflow` شو:
   - اول `mode: check` را اجرا کن.
   - وقتی سبز شد، یک‌بار `mode: live` را اجرا کن.

Secretهای قبلی با آپلود فایل‌ها پاک نمی‌شوند و لازم نیست دوباره ساخته شوند.

فایل یا پوشهٔ `.venv`، `.git` و `__pycache__` را آپلود نکن. داخل بستهٔ آماده هم وجود ندارند.

بعد از اجرای live، داخل چت خصوصی بات `/status` بفرست. بات این دستور را در اجرای بعدی جواب می‌دهد.

دو اجرای ناموفق ۲ اوت به‌خاطر آلبوم حجیم و خطای `413 Request Entity Too Large` بودند. نسخهٔ 3.1 آلبوم را بر اساس حجم تقسیم می‌کند و اگر لازم باشد هر فایل را جدا می‌فرستد؛ یک مدیای بد دیگر کل workflow را قرمز نمی‌کند.
